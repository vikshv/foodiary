import app from './app';
import template from './template.html';
import './style.less';

export default app.component('footer', {
    template,

    bindings: {
    },
    
    controller: function() {
        
    }
});
